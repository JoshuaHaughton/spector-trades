import { Card, CardContent} from '@mui/material';

export const IndividualAssets = () => (
    <Card
      sx={{ height: '100%' }}
    >
      <CardContent>
        Individual Assets
      </CardContent>
    </Card>
  );