import { Card, CardContent} from '@mui/material';

export const HeroGraph = () => (
    <Card
      sx={{ height: '100%' }}
    >
      <CardContent>
        Hero Graph
      </CardContent>
    </Card>
  );''