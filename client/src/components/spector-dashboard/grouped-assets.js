import { Card, CardContent} from '@mui/material';

export const GroupedAssets = () => (
    <Card
      sx={{ height: '100%' }}
    >
      <CardContent>
        Grouped Assets
      </CardContent>
    </Card>
  );