import {centsToDollars, niceMoney} from '../../utils/toHumanDollars';

const parseCryptoStats = (cryptoData, dashboardState) => {
  console.log("CryptoData: ", cryptoData)
  console.log("Dashboard State: ", dashboardState)
};
module.exports = {
  parseCryptoStats
};
